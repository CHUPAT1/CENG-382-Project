@page
@model CengReservation.Pages.Instructor.IndexModel
@{
    ViewData["Title"] = "Instructor Panel";
}

<div class="container mt-5">
    <h2 class="text-center text-primary mb-4">🎓 Instructor Panel</h2>

    <div class="d-flex flex-column align-items-center gap-3">
        <a asp-page="/Instructor/ReservationCalendar/Index" class="btn btn-outline-primary btn-lg w-50 text-start">
            📅 Reservation Calendar
        </a>
        <a asp-page="/Instructor/ClassroomList/Index" class="btn btn-outline-primary btn-lg w-50 text-start">
            🏫 Classroom List
        </a>
        <a asp-page="/Instructor/ReservationHistory/Index" class="btn btn-outline-primary btn-lg w-50 text-start">
            📝 Reservation History
        </a>
    </div>
</div>
